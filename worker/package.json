{
  "name": "zhika-gateway",
  "version": "0.1.0",
  "private": true,
  "description": "知卡内容摄取网关 + 订阅校验（Cloudflare Worker）",
  "type": "module",
  "main": "src/worker.js",
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
